apiVersion: v1
kind: Service
metadata:
  name: solo-leveling-gateway
spec:
  selector:
    app: solo-leveling-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http